"use strict";

module.exports = {
    // testing the sample for adding 2 numbers
    addTwoNumbers: function (first, second) {
        return first + second;
    },
    // testing the sample for subtracting 2 numbers
    subtractTwoNumbers: function (first, second) {
        return first - second;
    },
    // testing the sample for multiplying 2 numbers
    multiplyTwoNumbers: function (first, second) {
        return first * second;
    }
}